import { BashLine } from '~/components';

import { Container } from './styles';

const Home = () => {
  return (
    <Container>
      <BashLine></BashLine>
    </Container>
  );
};

export default Home;
